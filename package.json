{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.19.0",
    "typescript": "^3.7.3",
    "@types/node": "^12.12.21"
  },
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
  },
  "scripts": {
    "build": "tsc --build",
    "clean": "rimraf packages/*/{tsconfig.tsbuildinfo,lib,dist}"
  }
}
